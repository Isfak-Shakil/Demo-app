<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.materialdesign">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MaterialComponents.Light.NoActionBar">
<!--        android:theme="@style/Theme.MaterialDesign">-->
        <activity android:name=".ForgetPasswordSuccessActivity" />
        <activity android:name=".SetNewPasswordActivity" />
        <activity android:name=".ForgotPasswordSelectionActivity" />
        <activity android:name=".ForgotPasswordActivity" />
        <activity android:name=".OTPScreenActivity" />
        <activity android:name=".SignUp3rdAcitvity" />
        <activity android:name=".SignUp2ndPageActivity" />
        <activity android:name=".NewMaterialSignUpActivity" />
        <activity android:name=".WelcomeScreenActivity" />
        <activity android:name=".NewMaterialLoginScreen" />
        <activity android:name=".UserProfileActivity" />
        <activity android:name=".SignUpActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".DashBoardActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>